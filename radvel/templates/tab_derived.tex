\begin{deluxetable}{lrrr}
\tablecaption{ {{ title }} }
\tablehead{
  \colhead{Parameter} & 
  \colhead{Credible Interval} & 
  \colhead{Maximum Likelihood} & 
  \colhead{Units}
}
\startdata
{% for row in derived_rows %}
  {{ row }} \\
{% endfor %}
\enddata
\tablenotetext{}{$M_{p}\sin{i_{p}}$ is calculated under the assumption that $M_{p}\sin{i_{p}}<<M_{\star}$
\label{tab:derived_params}}
\end{deluxetable}
